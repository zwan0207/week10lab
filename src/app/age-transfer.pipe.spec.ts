import { AgeTransferPipe } from './age-transfer.pipe';

describe('AgeTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeTransferPipe();
    expect(pipe).toBeTruthy();
  });
});
